{
  "name": "@zod-compiler/trpc",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "client": "tsx src/client.ts",
    "build": "vp pack",
    "check": "zod-compiler check src/schemas.ts --json",
    "test": "vp test run"
  },
  "dependencies": {
    "@trpc/client": "^11.17.0",
    "@trpc/server": "^11.17.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "zod-compiler": "workspace:*"
  }
}
